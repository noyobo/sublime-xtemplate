<ul>
    <li>
        <a href="asf"></a>
        <div id="J_name">
            {{ #if (true) }}
                //code
            {{/if}}
            {{#if(x===1)}}
                //code
            {{elseif(x===2)}}
                //code
            {{else}}
                //code
            {{/if}}

            {{{block ('body')}}}
            {{~ username ~}}
            {{ user.name }}
            {{ user["name"] }}
            {{include ('header')}}
            {{include ('header')}}
        </div>
        <div class="name" id="" style="color: red">
            {{x}}
        </div>
        <audio src=""></audio>
    </li>
</ul>

<style>
    body{
        color: red;
    }
</style>

<div id="J_name"></div>

<script>
    function abc(acbc) {
        abc.afds
    }
    var a = a['32131']
    if (aa && a === 1 || (a + 1)) {

    }
</script>

<!-- 啊斯蒂芬森颠覆 -->